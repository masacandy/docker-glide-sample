# docker-go-es-nginx-sample

## Docker上のみでgolang + dep + ElasticSearch + Nginxを動かす

Docker for Macをインストール・起動後

```
docker-compose build
docker-compose up
```

`localhost:8080` にElasticSearchのバージョンが返ってきていたらok
